{
  "name": "noat-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/mobile", "packages/sync"],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile",
    "desktop": "npm run dev --workspace=apps/desktop",
    "format": "prettier --write .",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
